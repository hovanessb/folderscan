<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      The Bluetooth permission model changes at API 31. On Android 10/11 (API 29/30)
      the legacy BLUETOOTH/BLUETOOTH_ADMIN pair plus a location permission is what
      grants BLE scanning; from Android 12 onwards it is BLUETOOTH_SCAN/BLUETOOTH_CONNECT.
      Declaring both, bounded by maxSdkVersion, keeps one APK valid across the
      Android 10 -> 13 range this app supports.
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Required for BLE scanning below API 31; the CS108 is found via scan results. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Scoped storage from API 33 makes the broad read permission un-grantable. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13 gates notifications (WorkManager progress) behind a runtime grant. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Zebra DataWedge API access, required by newer DataWedge builds on the TC2xx. -->
    <uses-permission android:name="com.symbol.datawedge.permission.ACCESS_DATAWEDGE_API" />

    <!-- Not required: the Zebra TC210K has an integrated imager and needs no CS108. -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IgniteFolderScanner"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.IgniteFolderScanner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Retained so DataWedge profiles still configured for "Start Activity"
                 delivery keep working; the app now prefers broadcast delivery. -->
            <intent-filter>
                <action android:name="com.leonell.android.composefolderscanner.ACTION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />
    </application>

</manifest>
